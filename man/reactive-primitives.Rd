% Generated by tinyroxygen: do not edit by hand
% Please edit documentation in reactive.R
\name{reactive-primitives}
\alias{reactiveVal}
\alias{reactiveValues}
\alias{reactiveValuesToList}
\alias{reactive}
\alias{req}
\alias{isolate}
\alias{observe}
\alias{observeEvent}
\alias{eventReactive}
\alias{bindEvent}
\alias{tablerOptions}
\alias{bindCache}
\alias{syncUrl}
\title{Reactive Value}
\usage{
reactiveVal(value = NULL)
reactiveValues(...)
reactiveValuesToList(x, all.names = FALSE)
reactive(expr)
req(..., cancelOutput = FALSE)
isolate(expr)
observe(expr)
observeEvent(eventExpr, handlerExpr, ignoreInit = TRUE)
eventReactive(eventExpr, valueExpr, ignoreNULL = TRUE, ignoreInit = TRUE)
bindEvent(..., ignoreNULL = TRUE, ignoreInit = TRUE)
tablerOptions(cache)
bindCache(x, ...)
syncUrl(session, exclude = character(0L))
}

\arguments{
\item{value}{Initial value.}

\item{...}{Initial \code{name = value} pairs.}

\item{x}{A \code{reactiveValues()} object.}

\item{all.names}{Include names starting with a dot (default \code{FALSE}).}

\item{expr}{Expression (use \code{\{ \}} for multi-line).}

\item{cancelOutput}{Ignored (kept for signature compatibility with Shiny).}

\item{eventExpr}{Reactive expression whose change triggers the handler.}

\item{handlerExpr}{Expression to run when the event fires.}

\item{ignoreInit}{If \code{TRUE} (default), skip the first evaluation.}

\item{valueExpr}{Expression to evaluate (and cache) when the event fires.}

\item{ignoreNULL}{If \code{TRUE} (default), do not (re)compute while
  \code{eventExpr} evaluates to \code{NULL}.}

\item{cache}{A \pkg{tinycache} cache object (e.g.
  \code{tinycache::dcache(dir = "/path/to/cache")} or
  \code{tinycache::mcache()}) used to store \code{\link{bindCache}}
  values. If never set, an in-memory \code{tinycache::mcache()} is
  created and used automatically - which (like a plain
  \code{\link{reactive}}) does not survive an R restart. Pass a
  \code{dcache()} object to persist values across app restarts and
  sessions.}

\item{session}{The \code{session} object passed by \code{\link{tablerApp}}
  to the server function.}

\item{exclude}{Character vector of input IDs to omit from the URL.
  Action buttons are \emph{always} omitted regardless of this setting.}
}

\value{
A function that acts as getter/setter.
}

\description{
A single mutable reactive value.  Call with no argument to read
  (tracks the caller as a dependent); call with one argument to write (invalidates
  all current dependents).
}

