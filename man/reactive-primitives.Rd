% Generated by tinyroxygen: do not edit by hand
% Please edit documentation in reactive.R
\name{reactive-primitives}
\alias{reactive_val}
\alias{reactive_values}
\alias{reactive_values_to_list}
\alias{reactive}
\alias{req}
\alias{isolate}
\alias{observe}
\alias{observe_event}
\alias{event_reactive}
\alias{bind_event}
\alias{tabler_options}
\alias{bind_cache}
\alias{sync_url}
\title{Reactive Value}
\usage{
reactive_val(value = NULL)
reactive_values(...)
reactive_values_to_list(x, all.names = FALSE)
reactive(expr)
req(..., cancelOutput = FALSE)
isolate(expr)
observe(expr)
observe_event(event_expr, handler_expr, ignore_init = TRUE)
event_reactive(event_expr, value_expr, ignore_null = TRUE, ignore_init = TRUE)
bind_event(..., ignore_null = TRUE, ignore_init = TRUE)
tabler_options(cache)
bind_cache(x, ...)
sync_url(session, exclude = character(0L))
}

\arguments{
\item{value}{Initial value.}

\item{...}{Initial \code{name = value} pairs.}

\item{x}{A \code{reactive_values()} object.}

\item{all.names}{Include names starting with a dot (default \code{FALSE}).}

\item{expr}{Expression (use \code{\{ \}} for multi-line).}

\item{cancelOutput}{Ignored (kept for signature compatibility with Shiny).}

\item{event_expr}{Reactive expression whose change triggers the handler.}

\item{handler_expr}{Expression to run when the event fires.}

\item{ignore_init}{If \code{TRUE} (default), skip the first evaluation.}

\item{value_expr}{Expression to evaluate (and cache) when the event fires.}

\item{ignore_null}{If \code{TRUE} (default), do not (re)compute while
  \code{event_expr} evaluates to \code{NULL}.}

\item{cache}{A \pkg{tinycache} cache object (e.g.
  \code{tinycache::dcache(dir = "/path/to/cache")} or
  \code{tinycache::mcache()}) used to store \code{\link{bind_cache}}
  values. If never set, an in-memory \code{tinycache::mcache()} is
  created and used automatically - which (like a plain
  \code{\link{reactive}}) does not survive an R restart. Pass a
  \code{dcache()} object to persist values across app restarts and
  sessions.}

\item{session}{The \code{session} object passed by \code{\link{tabler_app}}
  to the server function.}

\item{exclude}{Character vector of input IDs to omit from the URL.
  Action buttons are \emph{always} omitted regardless of this setting.}
}

\value{
A function that acts as getter/setter.
}

\description{
A single mutable reactive value.  Call with no argument to read
  (tracks the caller as a dependent); call with one argument to write (invalidates
  all current dependents).
}

