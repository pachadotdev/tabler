% Generated by tinyroxygen: do not edit by hand
% Please edit documentation in inputs.R
\name{tabler-inputs}
\alias{select_input}
\alias{select_multiple_input}
\alias{slider_input}
\alias{update_select_input}
\alias{update_selectize_input}
\alias{update_slider_input}
\alias{text_input}
\alias{numeric_input}
\alias{date_input}
\alias{checkbox_input}
\alias{action_button}
\alias{checkbox_group_input}
\alias{radio_buttons}
\title{Select Input}
\usage{
select_input(input_id, label, choices, selected = NULL, searchable = TRUE, ...)
select_multiple_input(input_id, label, choices, selected = NULL, ...)
slider_input(
  input_id,
  label,
  min,
  max,
  value,
  step = 1,
  thumb_size = NULL,
  fill = FALSE,
  color = NULL,
  ...
)
update_select_input(session, input_id, label = NULL, choices = NULL, selected = NULL, ...)
update_slider_input(
  session,
  input_id,
  label = NULL,
  value = NULL,
  min = NULL,
  max = NULL,
  step = NULL
)
text_input(input_id, label, value = "", placeholder = NULL, mask = NULL, ...)
numeric_input(input_id, label, value, min = NULL, max = NULL, step = 1, ...)
date_input(
  input_id,
  label,
  value = Sys.Date(),
  min = NULL,
  max = NULL,
  icon = c("none", "left", "right"),
  inline = FALSE,
  ...
)
checkbox_input(input_id, label, value = FALSE, description = NULL, inline = FALSE, ...)
action_button(input_id, label, class = "btn-primary", icon = NULL, ...)
checkbox_group_input(input_id, label, choices, selected = NULL)
radio_buttons(input_id, label, choices, selected = NULL, inline = FALSE)
}

\arguments{
\item{input_id}{The input identifier used in \code{server}.}

\item{label}{Display label shown above the control.}

\item{choices}{Named or unnamed character vector of choices. A top-level
  element that is itself a vector of length > 1 is rendered as an
  \code{<optgroup>} (its name becomes the group label), matching Shiny's
  grouped-choices convention.}

\item{selected}{Initially-selected value (defaults to the first choice).}

\item{searchable}{If \code{TRUE} (the default), overlay the dropdown with
  a text box that filters choices by substring match anywhere in the
  label as the user types (e.g. typing "Korea" narrows the list to
  "South Korea", "North Korea", ...), instead of relying on the browser's
  native jump-to-prefix \code{<select>} behavior. Set to \code{FALSE} to
  render a plain native dropdown.}

\item{...}{Additional HTML attributes passed to the \code{<select>} element.}

\item{min}{Minimum value.}

\item{max}{Maximum value.}

\item{value}{Initial value. A length-2 vector renders a dual-thumb range
  slider.}

\item{step}{Step size (default \code{1}).}

\item{thumb_size}{Optional numeric multiplier of the default handle size
  (\code{1rem}), e.g. \code{1} for the normal size, \code{2} for double
  size, \code{2.5} for 250\%. Defaults to \code{NULL} (Tabler's built-in
  size).}

\item{fill}{If \code{TRUE}, paint the track from the minimum up to the
  current value (single slider) or between the two handles (range
  slider), instead of leaving the whole track a flat color.}

\item{color}{Optional accent color for the handle(s) and, when
  \code{fill = TRUE}, the filled track. Either one of Tabler's named
  theme colors (\code{"blue"}, \code{"azure"}, \code{"indigo"},
  \code{"purple"}, \code{"pink"}, \code{"red"}, \code{"orange"},
  \code{"yellow"}, \code{"lime"}, \code{"green"}, \code{"teal"},
  \code{"cyan"}) or any other valid CSS color. Defaults to \code{NULL},
  which uses the app's current primary/theme color (see \code{page()}).}

\item{session}{The session object.}

\item{placeholder}{Placeholder text (defaults to \code{mask}, if given).}

\item{mask}{Optional input mask made of \code{"0"} digit placeholders
  and literal separator characters, e.g. \code{"00/00/0000"} for a date
  or \code{"(00) 0000-0000"} for a phone number. As the user types
  digits, the literal characters are inserted automatically.}

\item{icon}{Where to place a calendar icon next to the field:
  \code{"none"} (default), \code{"left"} or \code{"right"}. Ignored when
  \code{inline = TRUE}.}

\item{inline}{If \code{TRUE}, render an always-visible month calendar
  (with previous/next-month navigation) instead of a text field with a
  popup picker.}

\item{description}{Optional secondary text shown below the label.}

\item{class}{Additional CSS classes (default \code{"btn-primary"}).}
}

\value{
An HTML tag.
}

\description{
A dropdown that lets the user pick one item from a list.
}

