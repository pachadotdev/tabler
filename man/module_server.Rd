% Generated by tinyroxygen: do not edit by hand
% Please edit documentation in module.R
\name{module_server}
\alias{module_server}
\title{Create a Namespaced Module Server}
\usage{
module_server(id, module, session = get_default_reactive_domain())
}

\arguments{
\item{id}{The module's namespace id (must match the id used to call the
  module's \code{*_ui} function).}

\item{module}{A function with signature \code{function(input, output, session)}.}

\item{session}{The parent session object. Defaults to the currently
  running \code{\link{tabler_app}} session (see \code{\link{get_default_reactive_domain}}).}
}

\value{
The return value of \code{module}, invisibly.
}

\description{
Dependency-free equivalent of \code{shiny::module_server()}.
  Wraps \code{input}/\code{output} so that unprefixed names accessed
  inside \code{module} are automatically namespaced with \code{id},
  matching the ids produced by \code{NS(id)} in the module's UI function.
}

